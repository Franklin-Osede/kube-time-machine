// Command ktm-agent runs inside a Kubernetes cluster, watches resources via
// client-go informers, and persists periodic snapshots to local storage.
//
// Etapa 1: this is just a placeholder so the project compiles. The real
// informer setup arrives in Etapa 2.
package main

import "fmt"

func main() {
	fmt.Println("ktm-agent: not implemented yet")
}
